# Lean dependencies for the LIVE paper-trading path (scripts/run_paper.py).
# Deliberately excludes the heavy backtest-only packages (cvxpy, vectorbt, simfin,
# duckdb, sklearn, seaborn, matplotlib) which are slow/painful to install on Windows
# and are not needed to run the daily paper strategy.
numpy>=1.26
pandas>=2.2
scipy>=1.12
yfinance>=0.2.40
ib_insync>=0.9.86
python-dotenv>=1.0
requests>=2.31
lxml>=5.0
